# IPv6 WireGuard Manager - 使用指南

## 概述

本指南将详细介绍IPv6 WireGuard Manager的各项功能和使用方法。该工具提供了完整的VPN服务器管理功能，包括服务器管理、客户端管理、网络配置、防火墙管理、系统维护、配置备份/恢复和更新检查。

## 目录

- [启动管理器](#启动管理器)
- [主菜单功能](#主菜单功能)
- [服务器管理](#服务器管理)
- [客户端管理](#客户端管理)
- [客户端安装](#客户端安装)
- [网络配置](#网络配置)
- [防火墙管理](#防火墙管理)
- [系统维护](#系统维护)
- [配置备份/恢复](#配置备份恢复)
- [更新检查](#更新检查)
- [BIRD版本管理](#bird版本管理)
- [故障排除](#故障排除)

## 启动管理器

### 运行方式选择

IPv6 WireGuard Manager 提供两种运行方式：

#### 方式1: 模块化版本（推荐）
```bash
./ipv6-wireguard-manager-core.sh
```
- **特点**: 轻量级核心脚本，按需加载功能模块
- **优势**: 启动更快，内存占用更少，便于维护
- **适用**: 日常使用和开发环境

#### 方式2: 完整版本
```bash
./ipv6-wireguard-manager.sh
```
- **特点**: 包含所有功能的完整脚本
- **优势**: 无需模块文件，单文件部署
- **适用**: 离线环境或简化部署

### 使用符号链接
```bash
# 使用完整命令
ipv6-wg-manager

# 或使用简写命令
wg-manager
```

**建议**: 推荐使用模块化版本，它提供了更好的性能和可维护性。

### 启动命令

```bash
# 使用模块化版本（推荐）
sudo ./ipv6-wireguard-manager-core.sh

# 或使用完整版本
sudo ./ipv6-wireguard-manager.sh

# 或使用符号链接
sudo ipv6-wg-manager
```

## 主菜单功能

启动管理器后，您将看到以下主菜单：

```
主菜单:
  1. 快速安装 (一键配置)
  2. 交互式安装
  3. 服务器管理
  4. 客户端管理
  5. 网络配置
  6. 防火墙管理
  7. 系统维护
  8. 配置备份/恢复
  9. 更新检查
  0. 退出
```

### 功能说明

#### 1. 快速安装 (一键配置)
- 使用默认配置快速部署VPN服务器
- 自动安装BIRD 2.x（如果可用）
- 自动配置防火墙规则
- 适合快速测试和部署

#### 2. 交互式安装
- 完整的配置向导
- 自定义所有参数
- 适合生产环境部署
- 支持自定义端口、IPv6前缀等

#### 3. 服务器管理
- 查看服务状态
- 启动/停止/重启服务
- 重载配置
- 查看服务日志
- 系统资源监控

#### 4. 客户端管理
- 添加/删除客户端
- 批量管理客户端
- 生成配置文件
- 客户端状态跟踪

#### 5. 网络配置
- IPv6前缀管理
- BGP邻居配置
- 路由表查看
- 网络诊断工具

#### 6. 防火墙管理
- 防火墙状态查看
- 规则管理
- 端口管理
- 服务管理

#### 7. 系统维护
- 系统状态检查
- 性能监控
- 日志管理
- 磁盘管理

#### 8. 配置备份/恢复
- 创建/恢复备份
- 自动备份设置
- 配置导入/导出

#### 9. 更新检查
- 版本检查
- 自动更新
- 更新日志

## 服务器管理

### 功能概述

服务器管理模块提供以下功能：

- 服务状态查看
- 启动/停止/重启服务
- 配置重载
- 服务日志查看
- 系统资源监控
- 网络连接状态

### 使用方法

1. 在主菜单选择 `3. 服务器管理`
2. 选择相应的子功能

### 子功能详解

#### 1. 查看服务状态
- 显示WireGuard服务状态
- 显示BIRD服务状态
- 显示防火墙服务状态
- 显示系统服务状态

#### 2. 手动启动服务
- 启动WireGuard服务
- 启动BIRD服务
- 启动防火墙服务
- 启动其他相关服务

#### 3. 手动停止服务
- 停止WireGuard服务
- 停止BIRD服务
- 停止防火墙服务
- 停止其他相关服务

#### 4. 手动重启服务
- 重启WireGuard服务
- 重启BIRD服务
- 重启防火墙服务
- 重启其他相关服务

#### 5. 重载配置
- 重载WireGuard配置
- 重载BIRD配置
- 重载防火墙配置
- 重载其他配置

#### 6. 查看服务日志
- 查看WireGuard日志
- 查看BIRD日志
- 查看防火墙日志
- 查看系统日志

#### 7. 系统资源监控
- CPU使用率
- 内存使用率
- 磁盘使用率
- 网络流量

#### 8. 网络连接状态
- 活动连接数
- 连接状态
- 流量统计
- 连接详情

#### 9. BIRD诊断工具 (新增功能)
- **综合诊断**: 全面检查BIRD安装、配置、服务和网络状态
- **安装诊断**: 检查BIRD安装状态、版本、控制工具和用户权限
- **配置诊断**: 验证配置文件语法、内容和权限设置
- **服务诊断**: 分析服务状态、systemd配置和启动失败原因
- **网络诊断**: 检查网络接口、IPv6转发和BGP连接状态
- **自动修复**: 自动修复权限问题、服务文件和网络配置
- **错误详情**: 显示详细的配置错误信息和修复建议

##### BIRD诊断工具使用方法

1. **启动诊断工具**
   ```bash
   ./ipv6-wireguard-manager-core.sh
   # 选择: 3. 服务器管理 → 9. BIRD诊断工具
   ```

2. **选择诊断类型**
   - 综合诊断（推荐）: 运行所有检查项目
   - 安装诊断: 专门检查安装问题
   - 配置诊断: 专门检查配置问题
   - 服务诊断: 专门检查服务问题
   - 网络诊断: 专门检查网络问题
   - 自动修复: 尝试自动修复问题
   - 错误详情: 显示详细错误信息

3. **诊断输出说明**
   - ✓ 绿色: 检查通过，无问题
   - ⚠ 黄色: 警告，需要注意但不影响功能
   - ✗ 红色: 错误，需要修复
   - 蓝色: 信息提示

4. **常见问题修复**
   - 权限问题: 自动提供修复命令
   - 配置错误: 显示具体错误位置和修复建议
   - 服务问题: 分析失败原因并提供解决方案
   - 网络问题: 检查网络配置和连通性

## 客户端管理

### 功能概述

客户端管理模块提供以下功能：

- 添加单个客户端
- 批量添加客户端
- 删除客户端
- 生成客户端配置
- 客户端状态跟踪
- QR码生成（移动设备）

### 使用方法

1. 在主菜单选择 `4. 客户端管理`
2. 选择相应的子功能

### 子功能详解

#### 1. 添加客户端
- 输入客户端名称
- 自动生成密钥对
- 分配IPv4和IPv6地址
- 生成配置文件

#### 2. 批量添加客户端
- 从CSV文件导入
- 批量生成配置
- 自动分配地址
- 生成安装脚本

#### 3. 删除客户端
- 选择要删除的客户端
- 确认删除操作
- 清理相关配置
- 更新服务器配置

#### 4. 生成客户端配置
- 选择客户端
- 生成配置文件
- 生成安装脚本
- 生成QR码

#### 5. 客户端状态跟踪
- 查看连接状态
- 查看流量统计
- 查看连接时间
- 查看IP地址

#### 6. QR码生成
- 生成客户端QR码
- 支持移动设备扫描
- 自动配置连接
- 保存QR码图片

## 客户端安装

### 支持的平台

IPv6 WireGuard Manager 支持多种客户端平台：

#### 桌面操作系统
- **Linux**: Ubuntu, Debian, CentOS, RHEL, Fedora, Rocky, AlmaLinux, Arch Linux
- **Windows**: Windows 10/11
- **macOS**: macOS 10.15+

#### 移动操作系统
- **Android**: Android 5.0+
- **iOS**: iOS 12.0+

### 安装方法

#### 方法一：自动安装脚本（推荐）

##### 1. 生成客户端配置包

在服务器上添加客户端时，系统会自动生成完整的配置包：

```bash
# 启动管理器
ipv6-wg-manager

# 选择：客户端管理 -> 添加客户端
# 输入客户端名称，选择自动分配地址
```

系统会自动生成以下文件：
- `config.conf` - WireGuard客户端配置文件
- `install.sh` - 自动安装脚本（Linux/Unix）
- `qr.png` - QR码（移动设备）
- `README.txt` - 安装说明

##### 2. Linux/Unix 系统安装

**自动安装（推荐）：**
```bash
# 1. 将配置包复制到客户端设备
scp -r /etc/ipv6-wireguard/clients/client_name/ user@client_ip:/tmp/

# 2. 在客户端设备上运行安装脚本
cd /tmp/client_name/
sudo ./install.sh
```

**手动安装：**
```bash
# 1. 安装 WireGuard
# Ubuntu/Debian
sudo apt update
sudo apt install -y wireguard

# CentOS/RHEL/Fedora
sudo yum install -y epel-release
sudo yum install -y wireguard-tools

# Arch Linux
sudo pacman -S wireguard-tools

# 2. 配置客户端
sudo mkdir -p /etc/wireguard
sudo cp config.conf /etc/wireguard/wg0.conf
sudo chmod 600 /etc/wireguard/wg0.conf

# 3. 启动 WireGuard
sudo systemctl enable wg-quick@wg0
sudo systemctl start wg-quick@wg0
```

##### 3. Windows 系统安装

**使用 WireGuard 官方客户端：**

1. **下载 WireGuard 客户端**
   - 访问：https://www.wireguard.com/install/
   - 下载 Windows 版本

2. **安装客户端**
   - 运行下载的安装程序
   - 按照提示完成安装

3. **导入配置**
   - 打开 WireGuard 客户端
   - 点击 "Add Tunnel" -> "Import from file"
   - 选择 `config.conf` 文件
   - 点击 "Add" 完成配置

4. **连接 VPN**
   - 在客户端列表中选择配置
   - 点击 "Activate" 连接

##### 4. macOS 系统安装

**使用 Homebrew 安装：**
```bash
# 1. 安装 WireGuard
brew install wireguard-tools

# 2. 配置客户端
sudo mkdir -p /etc/wireguard
sudo cp config.conf /etc/wireguard/wg0.conf
sudo chmod 600 /etc/wireguard/wg0.conf

# 3. 启动 WireGuard
sudo wg-quick up wg0
```

**使用官方客户端：**
1. 从 Mac App Store 下载 WireGuard 客户端
2. 导入 `config.conf` 配置文件
3. 点击连接按钮

#### 方法二：移动设备安装

##### 1. Android 设备

**安装步骤：**
1. 从 Google Play Store 安装 WireGuard 应用
2. 打开应用，点击 "+" 按钮
3. 选择 "Create from QR code"
4. 扫描服务器生成的 `qr.png` 文件
5. 点击 "Add Tunnel" 完成配置
6. 点击隧道名称旁边的开关连接

**手动配置：**
1. 打开 WireGuard 应用
2. 点击 "+" -> "Create from file or archive"
3. 选择 `config.conf` 文件
4. 点击 "Add" 完成配置

##### 2. iOS 设备

**安装步骤：**
1. 从 App Store 安装 WireGuard 应用
2. 打开应用，点击 "+" 按钮
3. 选择 "Create from QR code"
4. 扫描服务器生成的 `qr.png` 文件
5. 点击 "Add Tunnel" 完成配置
6. 点击隧道名称旁边的开关连接

**手动配置：**
1. 打开 WireGuard 应用
2. 点击 "+" -> "Create from file or archive"
3. 选择 `config.conf` 文件
4. 点击 "Add" 完成配置

#### 方法三：批量客户端安装

##### 1. 使用 CSV 文件批量添加

**创建客户端列表文件：**
```csv
# 客户端名称,IPv4地址,IPv6地址,描述
client1,auto,auto,测试客户端1
client2,10.0.0.100/32,2001:db8::100/128,指定地址客户端
mobile1,auto,auto,移动设备1
laptop1,auto,auto,笔记本电脑1
```

**在服务器上批量生成：**
```bash
ipv6-wg-manager
# 选择：客户端管理 -> 批量添加客户端
# 选择：使用CSV文件批量添加
# 输入文件路径：clients.csv
```

##### 2. 快速批量添加

```bash
ipv6-wg-manager
# 选择：客户端管理 -> 批量添加客户端
# 选择：快速批量添加
# 输入客户端数量：10
# 输入名称前缀：client
# 输入起始索引：1
```

### IPv6 子网段支持

#### 支持的子网段范围

客户端支持从 /56 到 /72 的灵活 IPv6 子网段分配：

| 网络前缀 | 客户端子网掩码 | 示例地址 | 说明 |
|---------|---------------|----------|------|
| /56     | /64          | 2001:db8::2/64 | 大型网络部署 |
| /57     | /65          | 2001:db8::2/65 | 中型网络部署 |
| /58     | /66          | 2001:db8::2/66 | 中型网络部署 |
| /59     | /67          | 2001:db8::2/67 | 小型网络部署 |
| /60     | /68          | 2001:db8::2/68 | 小型网络部署 |
| /61     | /69          | 2001:db8::2/69 | 小型网络部署 |
| /62     | /70          | 2001:db8::2/70 | 小型网络部署 |
| /63     | /71          | 2001:db8::2/71 | 小型网络部署 |
| /64     | /72          | 2001:db8::2/72 | 小型网络部署 |
| /65     | /73          | 2001:db8::2/73 | 超小型网络 |
| /66     | /74          | 2001:db8::2/74 | 超小型网络 |
| /67     | /75          | 2001:db8::2/75 | 超小型网络 |
| /68     | /76          | 2001:db8::2/76 | 超小型网络 |
| /69     | /77          | 2001:db8::2/77 | 超小型网络 |
| /70     | /78          | 2001:db8::2/78 | 超小型网络 |
| /71     | /79          | 2001:db8::2/79 | 超小型网络 |
| /72     | /80          | 2001:db8::2/80 | 超小型网络 |

#### 自动子网掩码分配

系统会根据服务器网络前缀自动确定客户端子网掩码：
- 检测服务器网络配置
- 计算合适的客户端子网掩码
- 自动分配避免冲突的地址

### 连接验证

#### 检查连接状态

**Linux/Unix/macOS：**
```bash
# 检查 WireGuard 状态
sudo wg show

# 检查网络接口
ip addr show wg0

# 测试连接
ping 10.0.0.1
ping6 2001:db8::1
```

**Windows：**
```bash
# 在 PowerShell 中检查
wg show

# 测试连接
ping 10.0.0.1
ping 2001:db8::1
```

**移动设备：**
- 打开 WireGuard 应用
- 查看连接状态和流量统计
- 测试网络连接

#### 网络测试

```bash
# 测试 IPv4 连接
ping 8.8.8.8

# 测试 IPv6 连接
ping6 2001:4860:4860::8888

# 测试 DNS 解析
nslookup google.com

# 检查路由表
ip route show
```

### 故障排除

#### 常见问题

##### 1. 连接失败

**检查项目：**
- 服务器是否在线
- 防火墙是否阻止连接
- 客户端配置是否正确
- 网络连接是否正常

**解决方法：**
```bash
# 检查服务器状态
ipv6-wg-manager
# 选择：服务器管理 -> 查看服务状态

# 检查客户端配置
sudo wg show

# 检查网络连接
ping SERVER_IP
```

##### 2. IPv6 连接问题

**检查项目：**
- 系统是否支持 IPv6
- 网络是否支持 IPv6
- 客户端配置中的 IPv6 地址

**解决方法：**
```bash
# 检查 IPv6 支持
ip -6 addr show

# 测试 IPv6 连接
ping6 2001:4860:4860::8888

# 检查 IPv6 路由
ip -6 route show
```

##### 3. DNS 解析问题

**检查项目：**
- DNS 服务器配置
- 网络连接
- 防火墙设置

**解决方法：**
```bash
# 测试 DNS 解析
nslookup google.com

# 更换 DNS 服务器
# 在客户端配置中修改 DNS 设置
```

##### 4. 移动设备连接问题

**检查项目：**
- 移动网络设置
- 应用权限
- 配置文件格式

**解决方法：**
- 重新扫描 QR 码
- 检查应用权限设置
- 重新导入配置文件

#### 重新生成配置

如果客户端配置有问题，可以在服务器上重新生成：

```bash
ipv6-wg-manager
# 选择：客户端管理 -> 重新生成客户端配置
# 输入客户端名称
# 选择重新生成选项
```

### 安全建议

#### 1. 密钥管理
- 定期轮换客户端密钥
- 安全存储私钥文件
- 不要共享私钥

#### 2. 网络安全
- 使用强密码保护设备
- 定期更新客户端软件
- 监控连接日志

#### 3. 配置管理
- 定期备份客户端配置
- 使用有意义的客户端名称
- 及时删除不需要的客户端

### 最佳实践

#### 1. 安装建议
- 优先使用自动安装脚本
- 定期更新客户端软件
- 备份重要配置文件

#### 2. 配置建议
- 使用自动地址分配避免冲突
- 配置合适的 DNS 服务器
- 设置合理的保持连接间隔

#### 3. 维护建议
- 定期检查连接状态
- 监控网络性能
- 及时处理连接问题

### 详细安装指南

完整的客户端安装说明请参考：[客户端安装指南](CLIENT_INSTALLATION.md)

## 网络配置

### 功能概述

网络配置模块提供以下功能：

- IPv6前缀管理
- BGP邻居配置
- 路由表查看
- 网络接口管理
- 网络诊断工具
- BGP状态查看
- 网络统计信息

### 使用方法

1. 在主菜单选择 `5. 网络配置`
2. 选择相应的子功能

### 子功能详解

#### 1. IPv6前缀管理

IPv6前缀管理提供完整的前缀生命周期管理功能：

##### 1.1 查看当前前缀
- 显示WireGuard配置中的IPv6地址
- 显示BIRD配置中的IPv6路由
- 实时查看当前生效的前缀配置

##### 1.2 添加新前缀
- 支持标准IPv6前缀格式（如：2001:db8::/48）
- 自动验证前缀格式有效性
- 自动将子网段转换为服务器地址（如：2001:db8::1/64）
- 同时更新WireGuard和BIRD配置
- 提供操作确认和结果反馈

##### 1.3 删除前缀
- 显示当前配置的前缀列表
- 支持选择性删除特定前缀
- 自动从WireGuard和BIRD配置中移除
- 提供删除确认机制

##### 1.4 修改前缀配置
- 支持前缀替换功能
- 输入旧前缀和新前缀
- 自动更新所有相关配置
- 保持配置一致性

##### 1.5 前缀统计信息
- **WireGuard统计**：显示配置的IPv6地址数量
- **BIRD统计**：显示宣告的IPv6路由数量
- **客户端统计**：显示IPv6客户端数量和地址使用情况
- **地址池状态**：显示可用地址和已用地址统计

##### IPv6地址配置说明
- **服务器地址**：使用具体IP地址（如2001:db8::1/64）
- **客户端地址**：从子网段中分配，支持/56到/72范围
  - /56网络 → 客户端使用/64子网掩码
  - /57网络 → 客户端使用/65子网掩码
  - /58网络 → 客户端使用/66子网掩码
  - ...以此类推...
  - /72网络 → 客户端使用/80子网掩码
- **BIRD路由**：宣告整个子网段（如2001:db8::/48）
- **自动转换**：管理器自动处理子网段到具体地址的转换
- **动态配置**：自动从WireGuard配置中获取当前网络设置

#### 2. BGP邻居配置
- 查看BGP邻居
- 添加BGP邻居
- 删除BGP邻居
- 修改邻居配置
- 邻居状态检查

#### 3. 路由表查看
- 查看IPv4路由表
- 查看IPv6路由表
- 查看BGP路由
- 路由统计信息

#### 4. 网络接口管理
- 查看接口详情
- 启用/禁用接口
- 配置接口IP
- 接口统计信息

#### 5. 网络诊断工具
- Ping测试
- 连通性测试
- DNS测试
- 端口扫描
- 延迟测试
- 路由跟踪

#### 6. BGP状态查看
- BGP会话状态
- BGP路由信息
- BGP统计信息
- BGP日志

#### 7. 网络统计信息
- 流量统计
- 连接统计
- 错误统计
- 性能统计

## 防火墙管理

### 功能概述

防火墙管理模块提供以下功能：

- 防火墙状态查看
- 启用/禁用防火墙
- 防火墙规则管理
- 端口管理
- 服务管理
- 防火墙日志查看

### 使用方法

1. 在主菜单选择 `6. 防火墙管理`
2. 选择相应的子功能

### 子功能详解

#### 1. 防火墙状态查看
- 查看防火墙状态
- 查看防火墙类型
- 查看防火墙版本
- 查看防火墙配置

#### 2. 启用/禁用防火墙
- 启用防火墙
- 禁用防火墙
- 切换防火墙状态
- 防火墙状态确认

#### 3. 防火墙规则管理
- 查看防火墙规则
- 添加防火墙规则
- 删除防火墙规则
- 修改防火墙规则

#### 4. 端口管理
- 查看开放端口
- 开放端口
- 关闭端口
- 端口扫描

#### 5. 服务管理
- 启动服务
- 停止服务
- 重启服务
- 切换服务状态

#### 6. 防火墙日志查看
- 查看防火墙日志
- 实时日志监控
- 日志过滤
- 日志分析

## 系统维护

### 功能概述

系统维护模块提供以下功能：

- 系统状态检查
- 性能监控
- 日志管理
- 磁盘空间管理
- 系统更新
- 进程管理
- 系统清理
- 安全扫描

### 使用方法

1. 在主菜单选择 `7. 系统维护`
2. 选择相应的子功能

### 子功能详解

#### 1. 系统状态检查
- 系统基本信息
- 服务状态检查
- 网络状态检查
- 磁盘状态检查

#### 2. 性能监控
- 实时监控
- CPU使用率监控
- 内存使用率监控
- 磁盘I/O监控
- 网络流量监控
- 进程资源监控

#### 3. 日志管理
- 查看系统日志
- 查看应用日志
- 检查日志大小
- 清理日志文件
- 实时日志监控

#### 4. 磁盘空间管理
- 显示磁盘使用情况
- 查找大文件
- 清理临时文件
- 清理包缓存

#### 5. 系统更新
- 检查系统更新
- 执行系统更新
- 执行安全更新
- 更新日志

#### 6. 进程管理
- 显示运行进程
- 查找进程
- 终止进程
- 进程详情
- 进程资源使用

#### 7. 系统清理
- 清理临时文件
- 清理日志文件
- 清理包缓存
- 清理系统垃圾

#### 8. 安全扫描
- 扫描开放端口
- 检查用户账户
- 检查文件权限
- 检查系统服务

## 配置备份/恢复

### 功能概述

配置备份/恢复模块提供以下功能：

- 创建配置备份
- 恢复配置备份
- 列出备份文件
- 删除备份文件
- 自动备份设置
- 导出/导入配置

### 使用方法

1. 在主菜单选择 `8. 配置备份/恢复`
2. 选择相应的子功能

### 子功能详解

#### 1. 创建配置备份
- 手动创建备份
- 选择备份内容
- 设置备份名称
- 备份进度显示

#### 2. 恢复配置备份
- 选择备份文件
- 确认恢复操作
- 恢复进度显示
- 恢复结果确认

#### 3. 列出备份文件
- 显示所有备份
- 备份文件详情
- 备份时间信息
- 备份大小信息

#### 4. 删除备份文件
- 选择要删除的备份
- 确认删除操作
- 删除结果确认

#### 5. 自动备份设置
- 显示自动备份设置
- 启用自动备份
- 禁用自动备份
- 设置备份频率
- 设置备份保留

#### 6. 导出/导入配置
- 导出配置到文件
- 从文件导入配置
- 配置验证
- 导入结果确认

## 更新检查

### 功能概述

更新检查模块提供以下功能：

- 检查更新
- 版本信息显示
- 更新管理器
- 系统包更新
- 更新日志
- 自动更新设置

### 使用方法

1. 在主菜单选择 `9. 更新检查`
2. 选择相应的子功能

### 子功能详解

#### 1. 检查更新
- 检查管理器更新
- 检查系统包更新
- 显示更新信息
- 更新建议

#### 2. 版本信息显示
- 显示当前版本
- 显示最新版本
- 版本比较
- 更新说明

#### 3. 更新管理器
- 下载更新
- 安装更新
- 更新进度显示
- 更新结果确认

#### 4. 系统包更新
- 检查系统包更新
- 执行系统包更新
- 更新进度显示
- 更新结果确认

#### 5. 更新日志
- 查看更新历史
- 更新详情
- 更新时间
- 更新内容

#### 6. 自动更新设置
- 显示自动更新设置
- 启用自动更新
- 禁用自动更新
- 设置更新频率
- 设置更新类型

## BIRD版本管理

### 支持的BIRD版本

- **BIRD 2.x** (推荐) - 默认安装版本，提供更好的性能和功能
- **BIRD 1.x** (兼容) - 自动回退版本，确保在BIRD 2.x不可用时仍能正常工作

### 版本选择策略

1. 优先尝试安装BIRD 2.x (`bird2`包)
2. 如果BIRD 2.x不可用，自动安装BIRD 1.x (`bird`包)
3. 支持所有主要Linux发行版

### 版本检测

系统会自动检测已安装的BIRD版本：

```bash
# 检查BIRD 2.x
which birdc2
birdc2 --version

# 检查BIRD 1.x
which birdc
birdc --version
```

### 服务管理

- 自动检测已安装的BIRD版本
- 支持BIRD 1.x和2.x的服务管理
- 兼容不同的控制台命令 (`birdc` vs `birdc2`)

### 控制命令

- **BIRD 2.x**: 使用 `birdc2` 命令
- **BIRD 1.x**: 使用 `birdc` 命令
- 系统会自动选择正确的命令

### 升级说明

如果需要升级BIRD版本：

1. 备份当前配置
2. 停止BIRD服务
3. 安装新版本BIRD
4. 迁移配置文件
5. 启动新版本服务
6. 验证配置

## 故障排除

### 常见问题

1. **权限问题**
   ```bash
   sudo ./ipv6-wireguard-manager-core.sh
   ```

2. **BIRD服务未启动**
   ```bash
   # 检查BIRD版本
   systemctl status bird2  # BIRD 2.x
   systemctl status bird   # BIRD 1.x
   
   # 启动服务
   sudo systemctl start bird2  # BIRD 2.x
   sudo systemctl start bird   # BIRD 1.x
   ```

3. **防火墙问题**
   ```bash
   # 检查防火墙状态
   sudo ufw status          # Ubuntu/Debian
   sudo firewall-cmd --state # CentOS/RHEL/Fedora
   ```

4. **端口占用**
   ```bash
   # 检查端口占用
   sudo netstat -tulpn | grep 51820
   ```

5. **模块文件缺失**
   ```bash
   # 检查模块文件
   ls -la modules/
   
   # 重新安装
   sudo ./install.sh
   ```

### 日志检查

```bash
# 查看系统日志
sudo journalctl -u wireguard
sudo journalctl -u bird2  # BIRD 2.x
sudo journalctl -u bird   # BIRD 1.x

# 查看管理器日志
tail -f /var/log/ipv6-wireguard-manager.log
```

### 配置检查

```bash
# 检查WireGuard配置
sudo wg show

# 检查BIRD配置
sudo birdc2 show protocols  # BIRD 2.x
sudo birdc show protocols   # BIRD 1.x

# 检查防火墙配置
sudo ufw status verbose
```

### 网络诊断

```bash
# 检查网络接口
ip addr show

# 检查路由表
ip route show
ip -6 route show

# 检查BGP状态
sudo birdc2 show protocols all  # BIRD 2.x
sudo birdc show protocols all   # BIRD 1.x
```

## 最佳实践

1. **定期备份配置**
2. **监控系统资源**
3. **检查服务状态**
4. **更新系统和软件**
5. **监控网络连接**
6. **检查防火墙规则**
7. **查看系统日志**
8. **使用模块化版本**

## 获取帮助

- **文档**: 查看 `docs/` 目录下的详细文档
- **示例**: 查看 `examples/` 目录下的配置示例
- **问题**: 在GitHub Issues中报告问题

---

**推荐**: 使用模块化版本 `./ipv6-wireguard-manager-core.sh` 获得最佳性能体验。
