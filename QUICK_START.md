# IPv6 WireGuard Manager - 快速启动指南

## 🚀 快速开始

### 1. 安装

```bash
# 下载安装脚本
wget https://raw.githubusercontent.com/ipzh/ipv6-wireguard-manager/main/install.sh
chmod +x install.sh

# 运行安装
sudo ./install.sh
```

### 2. 启动管理器

IPv6 WireGuard Manager 提供两种运行方式：

#### 方式1: 模块化版本（推荐）
```bash
./ipv6-wireguard-manager-core.sh
```
- **特点**: 轻量级核心脚本，按需加载功能模块
- **优势**: 启动更快，内存占用更少，便于维护
- **适用**: 日常使用和开发环境

#### 方式2: 完整版本
```bash
./ipv6-wireguard-manager.sh
```
- **特点**: 包含所有功能的完整脚本
- **优势**: 无需模块文件，单文件部署
- **适用**: 离线环境或简化部署

#### 使用符号链接
```bash
# 使用完整命令
ipv6-wg-manager

# 或使用简写命令
wg-manager
```

### 3. 快速安装VPN服务器

启动管理器后，选择：
1. **快速安装 (一键配置)** - 使用默认配置快速部署
2. **交互式安装** - 自定义配置参数

#### IPv6子网段支持
- **支持范围**: /56 到 /72 的IPv6子网段
- **自动分配**: 根据网络前缀自动确定客户端子网掩码
- **灵活配置**: 支持不同规模的网络部署需求

### 4. 管理功能

安装完成后，您可以使用以下功能：

#### 服务器管理
- 查看服务状态（WireGuard、BIRD、防火墙）
- 启动/停止/重启服务
- 重载配置
- 查看服务日志
- 系统资源监控
- 网络连接状态

#### 客户端管理
- 添加单个客户端
- 批量添加客户端
- 删除客户端
- 生成客户端配置
- 客户端状态跟踪
- QR码生成（移动设备）
- **自动安装脚本生成** - 支持多平台自动安装
- **移动设备支持** - Android/iOS 扫码配置
- **灵活子网段支持** - 支持/56到/72的IPv6子网段

#### 网络配置
- IPv6前缀管理
- BGP邻居配置
- 路由表查看
- 网络接口管理
- 网络诊断工具
- BGP状态监控

#### 防火墙管理
- 防火墙状态查看
- 规则管理（添加/删除）
- 端口管理
- 服务管理
- 防火墙日志查看

#### 系统维护
- 系统状态检查
- 性能监控
- 日志管理
- 磁盘空间管理
- 系统更新
- 进程管理
- 安全扫描

#### 配置备份/恢复
- 创建配置备份
- 恢复配置备份
- 自动备份设置
- 配置导入/导出
- 备份文件管理

#### 更新检查
- 版本检查
- 自动更新
- 更新日志

### 5. 客户端安装

#### 支持的平台
- **桌面系统**: Linux, Windows, macOS
- **移动设备**: Android, iOS
- **自动安装**: 支持自动生成安装脚本

#### 快速安装步骤

1. **在服务器上添加客户端**：
```bash
ipv6-wg-manager
# 选择：客户端管理 -> 添加客户端
# 输入客户端名称，选择自动分配地址
```

2. **在客户端设备上安装**：

**Linux/Unix（自动安装）：**
```bash
# 复制配置包到客户端
scp -r /etc/ipv6-wireguard/clients/client_name/ user@client_ip:/tmp/

# 运行自动安装脚本
cd /tmp/client_name/
sudo ./install.sh
```

**Windows：**
- 下载 WireGuard 客户端：https://www.wireguard.com/install/
- 导入 `config.conf` 配置文件

**移动设备：**
- 安装 WireGuard 应用
- 扫描生成的 QR 码

#### IPv6 子网段支持
- **支持范围**: /56 到 /72 的IPv6子网段
- **自动分配**: 根据网络前缀自动确定客户端子网掩码
- **灵活配置**: 支持不同规模的网络部署需求

#### 详细安装指南
完整的客户端安装说明请参考：[客户端安装指南](docs/CLIENT_INSTALLATION.md)

## 📋 重要说明

### BIRD版本
- **默认安装BIRD 2.x版本**，提供更好的性能和功能
- 如果BIRD 2.x不可用，自动回退到BIRD 1.x版本
- 支持所有主要Linux发行版

### 系统要求
- **支持的操作系统**: Ubuntu、Debian、CentOS、RHEL、Fedora、Rocky Linux、AlmaLinux、Arch Linux
- **需要root权限**运行
- **需要公网IPv4地址**
- **最低要求**: 1核心CPU、512MB内存、1GB磁盘空间

### 网络要求
- **公网IPv4地址**: 必需，用于客户端连接
- **IPv6地址**: 可选，用于IPv6前缀分发
- **开放端口**: WireGuard端口（默认51820/UDP）

## 🔧 主菜单功能

启动管理器后，您将看到以下主菜单：

```
主菜单:
  1. 快速安装 (一键配置)
  2. 交互式安装
  3. 服务器管理
  4. 客户端管理
  5. 网络配置
  6. 防火墙管理
  7. 系统维护
  8. 配置备份/恢复
  9. 更新检查
  0. 退出
```

### 功能说明

#### 1. 快速安装 (一键配置)
- 使用默认配置快速部署VPN服务器
- 自动安装BIRD 2.x（如果可用）
- 自动配置防火墙规则
- 适合快速测试和部署

#### 2. 交互式安装
- 完整的配置向导
- 自定义所有参数
- 适合生产环境部署
- 支持自定义端口、IPv6前缀等

#### 3. 服务器管理
- 查看服务状态
- 启动/停止/重启服务
- 重载配置
- 查看服务日志
- 系统资源监控

#### 4. 客户端管理
- 添加/删除客户端
- 批量管理客户端
- 生成配置文件
- 客户端状态跟踪

#### 5. 网络配置
- IPv6前缀管理
- BGP邻居配置
- 路由表查看
- 网络诊断工具

#### 6. 防火墙管理
- 防火墙状态查看
- 规则管理
- 端口管理
- 服务管理

#### 7. 系统维护
- 系统状态检查
- 性能监控
- 日志管理
- 磁盘管理

#### 8. 配置备份/恢复
- 创建/恢复备份
- 自动备份设置
- 配置导入/导出

#### 9. 更新检查
- 版本检查
- 自动更新
- 更新日志

## 🔍 故障排除

### 常见问题

1. **权限问题**
   ```bash
   sudo ./ipv6-wireguard-manager-core.sh
   ```

2. **BIRD服务未启动**
   ```bash
   # 检查BIRD版本
   systemctl status bird2  # BIRD 2.x
   systemctl status bird   # BIRD 1.x
   
   # 启动服务
   sudo systemctl start bird2  # BIRD 2.x
   sudo systemctl start bird   # BIRD 1.x
   ```

3. **防火墙问题**
   ```bash
   # 检查防火墙状态
   sudo ufw status          # Ubuntu/Debian
   sudo firewall-cmd --state # CentOS/RHEL/Fedora
   ```

4. **端口占用**
   ```bash
   # 检查端口占用
   sudo netstat -tulpn | grep 51820
   ```

5. **模块文件缺失**
   ```bash
   # 检查模块文件
   ls -la modules/
   
   # 重新安装
   sudo ./install.sh
   ```

## 📚 获取帮助

- **文档**: 查看 `docs/` 目录下的详细文档
- **示例**: 查看 `examples/` 目录下的配置示例
- **问题**: 在GitHub Issues中报告问题

## 🔄 更新

```bash
# 检查更新
ipv6-wg-manager
# 选择 "9. 更新检查"

# 或手动更新
git pull origin main
```

## 🎯 最佳实践

1. **使用模块化版本**: 推荐使用 `./ipv6-wireguard-manager-core.sh`
2. **定期备份**: 使用配置备份/恢复功能
3. **监控服务**: 定期检查服务状态
4. **更新系统**: 保持系统和软件更新
5. **安全配置**: 定期检查防火墙规则

---

**推荐**: 使用模块化版本 `./ipv6-wireguard-manager-core.sh` 获得最佳性能体验。