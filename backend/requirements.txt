# IPv6 WireGuard Manager - 核心依赖
# 已清理未使用的包，确保依赖与代码一致

# ============= FastAPI核心 =============
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# ============= 数据库 =============
sqlalchemy==2.0.23
alembic==1.13.1
PyMySQL==1.1.0
aiomysql==0.2.0

# ============= 缓存（可选）=============
redis==5.0.1

# ============= 认证和安全 =============
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
cryptography>=41.0.0,<47.0.0
pyotp>=2.9.0
qrcode[pil]>=7.4.2
Pillow>=10.0.0

# ============= HTTP客户端 =============
httpx==0.25.2

# ============= 监控和日志 =============
structlog==23.2.0
prometheus-client==0.19.0

# ============= 系统工具 =============
python-dotenv==1.0.0
psutil==5.9.6
click==8.1.7

# ============= 邮件验证 =============
email-validator==2.1.0

# ============= WebSocket支持（可选）=============
websockets==12.0
